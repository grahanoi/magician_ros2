version: '3.8'

services:
  ros_app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DISPLAY=host.docker.internal:0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix  # For X11 socket forwarding
    tty: true
    stdin_open: true

